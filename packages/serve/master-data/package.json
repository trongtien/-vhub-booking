{
  "name": "@booking/serve-master",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "db:migrate": "../../@booking/core/db.sh migrate",
    "db:rollback": "../../@booking/core/db.sh rollback",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --max-warnings 0",
    "generate:component": "turbo gen react-component",
    "check-types": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@booking/serve-core": "workspace:*"
  },
  "devDependencies": {
    "@booking/eslint-config": "workspace:*",
    "@booking/jest-config": "workspace:*",
    "@booking/typescript-config": "workspace:*",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "jest-extended": "^4.0.2",
    "ts-jest": "^29.1.2"
  }
}
